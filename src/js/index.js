const postsContainer = document.getElementById('posts-container');
const loader = document.querySelector('.loader'); //returns first element within document that matches specified selector
const filter = document.getElementById('filter');

let limit = 3;
let page = 1;







//*First bring in all DOM elements
//*Second create global variables limit, page
//*Third create function to fetch posts 

async function getPosts() {

}